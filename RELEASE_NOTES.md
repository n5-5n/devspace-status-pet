# DevSpace Status Pet v0.1.4-alpha.1

ペットが消えたように見える問題に対する、表示自己復旧と診断強化のPrereleaseです。

## 調査結果

発生時、DevSpace Status Petは終了していませんでした。

- 同じPIDで18:47から継続稼働
- プロセスは応答中
- `crash.log`なし
- `.NET Runtime`および`Application Error`なし
- 発生前にWindowsのSession Unlockと画面／電源復帰イベントあり

このため、最有力原因はモニター復帰時にレイヤー透過ウィンドウの表示状態または描画面が一時的に失われたことです。

## 再発防止

- モニター構成変更を検出して即時復旧
- スリープ／画面消灯からの復帰を検出して即時復旧
- 復帰2秒後に2回目の描画・位置補正
- 5秒ごとに表示、最前面、最小化、画面外状態を監視
- 描画失敗時にウィンドウハンドルを再生成して再試行
- タスクトレイへ「ペットを表示／復旧」を追加
- 画面外の保存位置をプライマリ画面内へ補正

## 診断ログ

通常動作を次へ記録します。

```text
%LOCALAPPDATA%\DevSpaceStatusPet\logs\runtime.log
```

記録内容：

- 起動と正常終了
- モニター構成変更
- 電源状態とResume
- 表示復旧の実行前後の座標
- `UpdateLayeredWindow`などの描画失敗とスタック情報

1MBを超えると`runtime.previous.log`へローテーションします。

## 検証

- Releaseビルド：警告0、エラー0
- 既存の全回帰テスト：成功
- 画面外位置の復旧：成功
- セカンダリーモニター上の正常位置維持：成功
- 日本語／英語の復旧メニュー：成功

これはPrereleaseです。実機でモニター復帰と表示自己復旧を確認してから、v0.1.4正式版へ昇格します。
