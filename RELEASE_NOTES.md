# DevSpace Status Pet v0.1.4

表示自己復旧と通常診断ログを正式採用した安定版です。

## 表示自己復旧

- モニター構成変更、スリープ／画面消灯からの復帰を検出
- 非表示、最小化、画面外移動、DWMクロークを監視
- WinFormsの管理値だけでなく、WindowsのネイティブTopMost状態を確認
- 描画失敗時にウィンドウハンドルを再生成して再試行
- タスクトレイの**ペットを表示／復旧**から手動復旧
- 低解像度モニターでは、設定倍率を保持したまま実描画倍率を画面内へ自動調整
- モニター間移動や表示復旧時も、移動先の作業領域へ再フィット

## 診断

通常動作を次へ記録します。

```text
%LOCALAPPDATA%\DevSpaceStatusPet\logs\runtime.log
```

起動、終了、電源・画面イベント、復旧理由、復旧前後の座標、管理／ネイティブTopMost状態、描画失敗を記録します。1MBを超えると`runtime.previous.log`へローテーションします。

未処理例外は次へ記録します。

```text
%LOCALAPPDATA%\DevSpaceStatusPet\logs\crash.log
```

## 安定版検証

- Releaseビルド：警告0、エラー0
- Classic／Neon、Light／Dark、3種類の吹き出し、3段階倍率の36組合せ描画
- 1024×768環境で250%表示を選んだ場合の画面内フィット
- 正方向／負方向の画面外移動、最小化、非表示、TopMost解除からの復旧
- 多重起動時の単一プロセス維持
- 壊れた設定JSON／位置JSONからの安全な起動
- 診断ログの1MBローテーション
- 60秒連続描画時のメモリ、ハンドル、スレッド、GDI／USERオブジェクト監視
- ZIP、SHA-256、実行ファイルバージョン、自己インストール／自己アンインストール
- GitHub ActionsのPR、main、タグ、Releaseワークフロー
- GitHub公開ZIPを再ダウンロードした後のSHA・インストール・実障害注入
- Windows Defender検査：脅威なし
- 既知の脆弱なNuGetパッケージ：なし
- 公開物へのローカルユーザー名・非公開プロジェクト名の混入なし

v0.1.3の設定、インストール先、自動起動設定はそのまま引き継がれます。
