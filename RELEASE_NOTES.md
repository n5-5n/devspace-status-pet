# DevSpace Status Pet v0.1.4-alpha.2

v0.1.4正式版へ昇格する前の、表示自己復旧と診断機能の安定版候補です。

## 修正

- Windows側でネイティブのTopMost状態だけが外れた場合も検出して自動復旧
- WinFormsの`TopMost`プロパティが`true`のままでも、実際の`WS_EX_TOPMOST`を確認
- 起動直後にネイティブTopMostを明示的に再適用
- 復旧ログへ管理状態とネイティブTopMost状態の両方を記録

## テスト強化

- 正方向／負方向の画面外移動
- 最小化、非表示、ネイティブTopMost解除
- 5回の多重起動と単一プロセス維持
- Classic／Neon、Light／Dark、3種類の吹き出し、3段階の倍率による36組合せ描画
- 壊れた設定JSONと位置JSONからの安全な起動
- 1MB超の`runtime.log`ローテーション
- 公開ZIP、SHA-256、実行ファイルバージョン、自己インストール／自己アンインストール
- GitHub Releaseへの実通信更新確認と公開パッケージ検証
- 60秒連続描画時のメモリ、ハンドル、スレッド、GDI／USERオブジェクト監視
- Windows Defender検査とNuGet脆弱性検査
- 公開物へのローカルユーザー名・非公開プロジェクト名の混入検査

## 検証結果

- Releaseビルド：警告0、エラー0
- 全自動回帰テスト：成功
- 実機障害注入：全項目自動復旧
- 60秒連続描画：応答維持、GDI／USERオブジェクトの継続増加なし
- Windows Defender：脅威なし
- 既知の脆弱なNuGetパッケージ：なし

このPrereleaseのGitHub公開物を再検証し、同一コードで問題がなければv0.1.4正式版へ昇格します。
