# DevSpace Status Pet v0.1.6-alpha.3

`v0.1.6-alpha.2`でも長時間稼働後にメモリ使用量が再び数GBへ増える問題を修正したHotfix Prereleaseです。

## 原因

DevSpaceの子プロセスを探す処理で、PC上の全プロセスに対して毎秒`Process.MainModule`、開始時刻、CPU時間を取得していました。

保護されたシステムプロセス、Java、QEMU、Codexなどではアクセス拒否となり、例外を握りつぶして処理を継続していました。実機では次の異常が確認されました。

- 1回のプロセス走査で約404件のFirstChance例外
- 毎秒約410件、2分で約51,400件の例外
- 約49分後にWorking Set約2.08GB、Private Bytes約2.15GB
- Peak Working Set約3.11GB
- 30秒で約40秒分のCPU時間を消費

GCヒープの生存データは数MBでしたが、大量の例外・スタックトレース生成と割り当てが長時間続き、プロセスメモリが膨張していました。

## 修正

- 全プロセス一覧はToolhelp32 APIでPID・親PID・実行名だけを例外なしで取得
- DevSpace配下の少数プロセスだけ詳細取得
- `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)`、`QueryFullProcessImageNameW`、`GetProcessTimes`を使用
- API失敗は例外化せず、安全な既定値として扱う
- 子孫探索へPID重複防止を追加
- 実行ファイルパス用バッファを通常1,024文字へ縮小し、必要時だけ32,768文字へ拡張
- `v0.1.6-alpha.2`の再利用DIB描画修正も維持

## 修正効果

同じ全プロセス走査20回の比較：

- FirstChance例外：8,091件 → 0件
- アクセス拒否例外：7,980件 → 0件
- 所要時間：1.87秒 → 0.21秒

500回連続走査：

- 例外：0件
- Private Bytes：16.6MB → 12.6MB
- Working Set：35.3MB → 35.2MB
- ハンドル：216 → 218
- 所要時間：旧59.2秒 → 新6.3秒

実アプリのRuntimeカウンター比較：

- 例外数：約410件/秒 → 0件/秒
- 割り当て速度：約1.18MB/秒 → 約0.20MB/秒
- CPU使用率：平均約2.50% → 約1.54%
- GCヒープ：約4MBで安定

通常環境での4分間監視：

- Working Set：152.3MB → 150.2MB
- Private Bytes：67.3MB → 64.6MB
- Private最大：72.8MB
- GDI：52～53
- USER：42～43
- 応答停止なし

## 回帰テスト

- 全プロセス走査でFirstChance例外0件をCIで確認
- 子プロセスの検出、パス、開始時刻を例外なしで取得
- 500回プロセス走査ストレス
- 4MBログの反復解析
- 4,000フレーム・333回サイズ変更の描画ストレス
- 再利用DIB、透明度、アンチエイリアス
- 日本語・英語・簡体字の108通り描画
- 画面端収納、表示復旧、低解像度、更新、インストール回帰

`v0.1.6-alpha.1`および`v0.1.6-alpha.2`は長時間稼働させず、この版へ更新してください。既存の設定、位置、自動起動登録、端収納機能はそのまま引き継がれます。
